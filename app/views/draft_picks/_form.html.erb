<%= form_for(@draft_pick) do |f| %>
  <% if @draft_pick.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@draft_pick.errors.count, "error") %> prohibited this draft_pick from being saved:</h2>

      <ul>
      <% @draft_pick.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :draft_id %><br>
    <%= f.select :draft_id, options_from_collection_for_select(Draft.all.order('id'), "id", "id", @draft_pick.draft_id) %>
  </div>
  <div class="field">
    <%= f.label :fantasy_team_id %><br>
    <%= f.select :fantasy_team_id, options_from_collection_for_select(FantasyTeam.all.order('name'), "id", "name", @draft_pick.fantasy_team_id) %>
  </div>
  <div class="field">
    <%= f.label :player_id %><br>
    <%= f.select :player_id, @players %>
  </div>
  <div class="field">
    <%= f.label :price %><br>
    <%= f.number_field :price %>
  </div>
  <div class="field">
    <%= f.label :round %><br>
    <%= f.number_field :round %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
